## v1.0.7 更新内容

### 系统代理支持

- Windows 启动时自动读取当前用户的静态系统代理和绕过列表。
- 额度刷新、官方 Token 刷新以及 API/官方可用性测试统一使用系统代理。
- 未配置系统代理时继续支持 `HTTP_PROXY` 和 `HTTPS_PROXY` 环境变量。

## v1.0.6 更新内容

### 模型信息显示

- 官方账号列表中的模型名称改为读取当前 Codex 实际使用的 `config.toml`，不再依赖可能过期的托管配置。
- 修复 Codex 升级模型后列表仍显示旧模型的问题，现可正确显示 `gpt-5.6-sol` 等当前模型。
- 仅更新列表展示结果，不覆盖官方共享配置，避免影响自定义 `base_url` 和账号切换行为。

### 归档对话清理

- 清理超过保留期限的归档对话文件时，同步删除 Codex 状态数据库中的对应索引，避免已删除对话继续出现在列表中。
- 支持清理文件已经不存在、但仍残留在状态数据库中的过期归档记录。
- 仅处理 `archived_sessions` 目录内的过期归档，保留未过期记录以及目录外的数据。
- 增加归档文件与状态数据库索引联动清理的回归测试。
